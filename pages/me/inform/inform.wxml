<!--pages/me/inform/inform.wxml-->
<view class='container'>
  <view>
    <view wx:for="{{ data }}" wx:key="{{item.id}}" data-id='{{ item.ID }}' class='item bb' bindtap='bindOpenInfo'>
      <view>{{ item.Title }}</view>
      <view>时间：{{ item.AddTime }}</view>
    </view>
  </view>
</view>